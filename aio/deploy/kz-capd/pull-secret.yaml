# lab patch: ONLY the ghcr pull secret — none of the kz-eu oidc wiring (no
# kube-oidc-proxy CA / dashboard-kubeconfig here; token login from the base).
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kubernetes-dashboard
spec:
  template:
    spec:
      imagePullSecrets:
        - name: lazedo-ghcr
