# Ignore all development artifacts so that the build is always clean.
.git
.tmp/
.tools/
dist/
node_modules/
# The Angular build cache must never enter the image build — a stale copied
# cache makes the in-image ng build emit outdated bundles.
.angular/
